[package]
name = "greeks"
version = "0.1.0"
edition = "2021"

[dependencies]
derivatives = { path = "../derivatives" }
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
# statrs provides the standard Normal distribution (CDF / PDF)
statrs = "0.17"

[dev-dependencies]
approx = "0.5"
